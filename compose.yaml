services:
  api:
    build: .
    container_name: ml-ticket-classifier-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./database:/app/database
      - ./models:/app/models
      - ./reports:/app/reports
    command: uvicorn src.api:app --host 0.0.0.0 --port 8000